// This screen is hidden from the tab bar (href: null in _layout.tsx).
// Kept as a minimal placeholder to avoid routing errors.
import { View } from 'react-native';

export default function ExploreScreen() {
  return <View />;
}
